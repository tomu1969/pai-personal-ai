# OpenAI Configuration (loads from parent project)
# The system will automatically load the API key from ../ai_pbx/.env
# No need to duplicate the key here

# Gmail API Configuration (for sending pre-qualification letters)
GMAIL_CREDENTIALS_PATH=credentials.json
GMAIL_TOKEN_PATH=token.pickle
GMAIL_SENDER_EMAIL=noreply@premiermortgage.com

# Company Information for Pre-Qualification Letters
COMPANY_NAME=Premier Mortgage Services
COMPANY_PHONE=1-800-MORTGAGE
COMPANY_EMAIL=info@premiermortgage.com
LOAN_OFFICER_NAME=John Smith
NMLS_NUMBER=123456
LETTER_EXPIRY_DAYS=90

# Fallback SMTP Configuration (optional - if Gmail OAuth2 fails)
# Use an app-specific password for Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Server Configuration
PORT=8000
HOST=0.0.0.0